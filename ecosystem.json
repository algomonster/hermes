{
  apps : [
    {
      name      : "Hermes",
      script    : "bin/www",
      env: {
        /* COMMON_VARIABLE: "true" */
      },
      env_production : {
        NODE_ENV: "production"
      }
    },
  ],
  deploy : {
    production : {
      user : "node",
      host : "algotrading.space",
      ref  : "origin/master",
      repo : "https://github.com/algomonster/hermes.git",
      path : "/www/node/hermes",
      "post-deploy" : "npm install ; pm2 startOrRestart ecosystem.json --env production"
    }
  }
}